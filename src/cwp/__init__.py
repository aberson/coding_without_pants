"""coding_without_pants — the ``cwp`` CLI package.

One CLI, two loops: the Channel Loop (idea → published) and the Pantsless Build
(kid speaks → AI builds → verified toy). See plan.md at the repo root.
"""

__version__ = "0.1.0"
